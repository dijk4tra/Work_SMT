# 简历能力与代码证据矩阵

| 简历主要工作 | 代码与测试证据 | 状态 |
|---|---|---|
| Wfrest 接入与 Search Server 双进程 | `search_controller.cpp`、`logtrace_gateway.cpp`、HTTP E2E | 完成 |
| Protobuf/SRPC 和错误映射 | `proto/logtrace.proto`、RPC client/service、健康与业务 E2E | 完成 |
| 结构化元数据、倒排索引、低 DF AND、Top-K | Segment Store、Search Engine 和固定排序测试 | 完成 |
| 增量批次和 READY 原子发布 | Incremental Indexer、Segment Manager、恢复与损坏 E2E | 完成 |
| BM25 与业务权重 | `search_engine.cpp` 及相关性单元测试 | 完成 |
| offset/length 与 pread 原文回读 | Document/File 表、Original Log Reader、详情 E2E | 完成 |
| SLRU 与 Redis 分层缓存 | SLRU/Query Cache、并发测试、Redis 命中和故障回退 E2E | 完成 |

跨项目职责证据：一期 Collector 和 DataStream 生成可信归档；二期只读消费 `archive_file` 和归档目录，
没有上传、设备认证或归档写入代码。发布证据由 `scripts/acceptance.sh` 和第六期验收报告汇总。
