# 一期归档元数据库的只读账号密码。
SMT_LOGTRACE_SOURCE_MYSQL_PASSWORD=change_me

# 二期索引状态数据库的读写账号密码。
SMT_LOGTRACE_STATE_MYSQL_PASSWORD=change_me

# 仅在生产 Redis 启用认证时设置；本地开发配置不读取该变量。
SMT_LOGTRACE_REDIS_PASSWORD=change_me
