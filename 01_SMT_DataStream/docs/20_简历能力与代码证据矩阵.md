# 简历能力与代码证据矩阵

## 1. 使用规则

本矩阵逐条对应根目录 `SMT产线项目.md` 中“数据采集与归档管理平台”的主要工作。状态只有在
实际源码、自动化测试和可重复命令都存在时才能标为“完成”；只有设计文档的条目保持“待实现”。

## 2. 项目主要工作映射

| 简历条目 | 当前证据 | 状态 | 计划完成阶段 |
|---|---|---|---|
| Wfrest 设备认证、心跳、上传和查询接口 | `heartbeat_controller.cpp`、`upload_controller.cpp`、`archive_controller.cpp`；HTTP E2E | 完成 | 第一至第四期 |
| Workflow 编排设备、Redis 和 MySQL 任务 | 认证、心跳、分片、完成和查询异步链；客户端与 E2E | 完成 | 第二至第四期 |
| OpenSSL HMAC-SHA256、正文摘要、时间窗口和防重放 | `crypto.cpp`、`device_authenticator.cpp`；已知向量和 E2E 负向测试 | 完成 | 已在第二期完成 |
| 分片上传、`posix_fallocate`、`pwrite`、Bitmap 和断点续传 | `upload_controller.cpp`、`upload_repository.cpp`、并发上传 E2E | 完成 | 第三期 |
| 窗口化 `mmap` 和整文件 SHA-256 | `archive_storage.cpp`；非页对齐窗口、大小和摘要单测；全量样本 E2E | 完成 | 第四期 |
| 同文件系统 `rename`、完成幂等和唯一约束 | 确定性路径、原子移动、两类崩溃窗口故障注入 E2E | 完成 | 第四期 |
| 文件元数据表和高频联合索引 | `archive_repository.cpp`、Operator 列表/详情、稳定游标和全量样本查询 | 完成 | 第四期 |
| Redis 心跳、上传会话和清理 | 心跳、会话、Bitmap、配额释放及严格旧临时文件清理 | 完成 | 第二至第四期 |
| 采集程序发现文件并可靠上传 | `collector_agent`、原子 spool、三种封口、退避和真实闭环 E2E | 完成 | 第五期 |
| Linux 生产部署与质量门禁 | Nginx TLS、systemd、logrotate、覆盖率、Sanitizer、Valgrind、压测与备份演练 | 完成 | 第六期 |

## 3. 已完成代码证据

### 3.1 设备认证与心跳

- `include/datastream/auth/crypto.h`、`src/auth/crypto.cpp`：SHA-256、HMAC-SHA256、规范串和
  常量时间比较；
- `src/auth/device_authenticator.cpp`：请求头、时间窗口、MySQL 设备状态和 Redis 防重放；
- `src/api/heartbeat_controller.cpp`：心跳模型、MySQL 更新时间和 Redis Hash+TTL；
- `tests/unit/crypto_test.cpp`：密码学已知向量；
- `tests/e2e/heartbeat_e2e_test.py`：正确签名、篡改、过期、重放、未知/停用设备、TTL 和日志脱敏。

验证命令：

```bash
export SMT_DATASTREAM_MYSQL_PASSWORD='123456'
export SMT_DATASTREAM_OPERATOR_TOKEN='e2e-operator-token-2026'
ctest --test-dir build --output-on-failure
```

第二期当前记录为 Debug 全量 31/31 通过。

### 3.2 元数据和索引

- `migrations/001_initial_schema.sql`：产线、工位、设备和归档元数据表；
- `archive_file` 唯一约束：`upload_id`、`relative_path`；
- 联合索引：设备时间、工位时间、工单与产品 SN 时间；
- `scripts/db.sh`：显式版本迁移和 checksum 防篡改。

第四期新增 `archive_repository.cpp` 和 `archive_controller.cpp`，已经通过设备/工单/UTC 范围组合
查询、两页游标、详情查询和 63 条固定样本逐条核对。索引选择的 `EXPLAIN` 结果记录在
`14_第四期开发报告.md`。

## 4. 后续每期更新要求

- 第三期分片偏移、Bitmap、重复分片、断点续传和并发写证据已完成；
- 第四期 `mmap`、`rename`、MySQL 唯一记录恢复、查询和清理证据已完成；
- 第五期已完成文件稳定判定、spool、断网、强退、Redis 会话丢失与重启恢复真实证据；
- 第六期已完成 Nginx/systemd TLS 部署、30 RPS 固定负载、Valgrind、故障注入、备份和三线
  演示命令；
- 每次更新保持“简历描述、代码状态、测试报告”一致，不把计划能力提前标为完成。
